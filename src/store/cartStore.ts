// @deprecated — This file is kept as a re-export shim for backwards compatibility.
// Use `@/store/CartProvider` directly instead.
export { CartProvider, useCartStore, type CartItem } from "./CartProvider";
